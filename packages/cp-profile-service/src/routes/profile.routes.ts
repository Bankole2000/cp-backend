import { Router } from 'express';

const router = Router();

router.get('/:username'); // Get user profile
router.post('/:username/follow'); // Follow user / if user is private or blocked, send follow request
router.post('/:username/follow'); //
router.post('/:username/');
router.post('/:username/');
router.post('/:username/');
router.post('/:username/');

export { router as profileRoutes };
